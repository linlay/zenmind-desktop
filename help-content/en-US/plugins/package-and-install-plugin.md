1. Make sure the plugin root directory contains a valid `manifest.json`.
2. Package the plugin directory as a `{{pluginArchiveLabel}}` file:

```text
{{pluginArchiveCommand}}
```

3. In [Control Center](/control-center), click "Install Plugin" and choose the package file.
4. After installation, the plugin appears immediately in the Control Center service list.
