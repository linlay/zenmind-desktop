Check the following:

- The plugin package (`{{pluginArchiveLabel}}`) must contain a valid `manifest.json` file.
- The `kind` field in `manifest.json` must be `"plugin"`.
- Confirm that the plugin ID does not conflict with an installed plugin.
- Check the main process logs for loading errors.
