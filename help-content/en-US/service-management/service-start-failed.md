1. Check whether the service status is `config-required`. If so, complete the configuration first.
2. Open the service logs from the detail card's "Logs" tab and look for the concrete error.
3. Confirm dependency services are running. For example, `agent-platform` depends on the container hub.
4. Try the "Restart" button. A leftover process can sometimes prevent startup.
