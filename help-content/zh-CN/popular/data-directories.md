新安装的应用运行数据存储在桌面端分层数据目录中：

- **macOS**：`~/.zenmind/.desktop/`
- **Windows**：`%USERPROFILE%\.zenmind\.desktop\`

该目录按 `config`、`data`、`state`、`logs`、`cache`、`secrets` 和 `profiles` 分层；服务和插件程序位于系统 Application Support 的 ZenMind 目录。
