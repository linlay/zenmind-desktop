新安装的应用运行数据存储在桌面端分层数据目录中：

- **macOS**：`{{runtimeDataPathMac}}`
- **Windows**：`{{runtimeDataPathWindows}}`

该目录按 `config`、`data`、`state`、`logs`、`cache`、`secrets` 和 `profiles` 分层；服务和插件程序在 macOS 位于 `{{programDataPathMac}}`，在 Windows 位于 `{{programDataPathWindows}}`。
